<%= simple_form_for(@task) do |f| %>
  <%= f.input :title %>
  <%= f.input :details %>
  <%= f.input :completed unless @task.new_record? %>
  <%= f.submit %>
<% end %>
<hr>

<%= link_to tasks_path do %>
  <%= fa_icon "undo", text: "Back to list" %>
<% end %>
